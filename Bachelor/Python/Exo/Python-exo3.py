L = ['abcabcabcabcabcabcabcabcabc']

def solution(mms):
    for size_slice in range(1,len(mms)):
        for other_slice in range(size_slice,len(mms),2):
            if mms[0:size_slice] != mms[?:?+size_slice]
    
    return len(mms) / size_slice
                                 